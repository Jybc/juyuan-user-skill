---
slug: juyuan-user-skill
name: juyuan-user-skill
version: 1.4.0
displayName: 聚源百成大师
description: 聚源百成大师 — 通过聚源百成MCP 把开山网(K3)/包牛牛(Bao66)产品一键发到淘宝。管店、改价、发货、回评、优化标题，一个skill全搞定。
tags: [k3, 开山网, 包牛牛, 极速发布, 电商, 鞋靴]
license: MIT
homepage: https://github.com/Jybc/juyuan-user-skill
---

# 聚源百成大师

把开山网(K3)/包牛牛(Bao66)的产品一键发布到淘宝，管理商品、订单、评价、退款。支持标题AI优化和SEO深度诊断。

## 能力总览

以下为 Skill 可提供的全部能力。**对话开始时或用户询问"你能做什么"时，主动向用户介绍；每次对话结束前，结合上下文提示用户接下来可以做什么**。

### 选品发布
| 能力 | 说明 | 触发示例 |
|------|------|----------|
| 今日新款 | 查看开山网/包牛牛当日新品，移动端展示页 | 「看看今天新款」「有新品吗」 |
| 搜索产品 | 按关键词搜货源（2~20 字符，安全过滤） | 「搜凉鞋」「找一个厚底拖鞋」 |
| 店铺列表 | 查看已绑定淘宝店铺 | 「我的店铺」 |
| 极速发布 | 选中产品一键发布到淘宝店铺 | 「把这个发到xx店铺」 |
| 发布记录 | 查看/导出历史发布记录 | 「发布记录」「导出发布」 |

### 店铺管理
| 能力 | 说明 | 触发示例 |
|------|------|----------|
| 商品管理 | 在售/仓库列表、上下架、改价、删除 | 「看下在售商品」「下架xx」 |
| 订单管理 | 订单列表/详情、发货、改地址、备注 | 「待发货订单」「帮我发货」 |
| 评价管理 | 评价列表、回复、差评告警 | 「看看评价」「回评」「有没有差评」 |
| 退款管理 | 退款列表、同意/拒绝、退货、拦截 | 「看看退款」「同意这笔退款」 |

### 批量运营
| 能力 | 说明 | 触发示例 |
|------|------|----------|
| 仪表盘 | 店铺经营全貌 | 「仪表盘」 |
| 日报 | 每日经营日报 | 「今天日报」 |
| 批量发货 | 一次性处理所有待发货订单 | 「待发货的都发了」 |
| 批量调价 | 全场折扣/加价/改价（+10% / -5 / 99.00） | 「全场 9 折」「涨价 5 块」 |
| 批量标题优化 | 前缀/后缀/替换批量改标题 | 「批量优化标题」 |
| 标题巡检 | 全店标题质量巡检 | 「标题巡检」 |
| 差评告警 | 中差评筛查与提醒 | 「评价巡检」 |

### AI 能力
| 能力 | 说明 |
|------|------|
| 标题优化 | 场景驱动 + 热搜词库 + SEO 自检，生成符合2026淘宝算法的标题 |
| SEO 诊断 | 四维度分析：热搜覆盖 / 词序评分 / 竞争分析 / 改进建议 |
| 利润分析 | 基于拿货价/成本算单品净利与利润率 |
| 经营问答 | 自然语言问答店铺经营数据 |
| 属性补全 | 自动补全商品缺失属性 |
| 选品日历 | 按季节/热度推荐应季选品 |
| 详情文案 | 生成商品详情页文案 |

## 交互原则

- 获取 API Key 用 `AskUserQuestion` 引导，不暴露配置文件路径
- **API 调用**: 优先使用 MCP Tool（服务端聚源百成MCP，实例名 `开山网xx`/`包牛牛xx`），失败/超时/不可用 → 降级 `scripts/driver.py`
- curl 仅在 Python 不可用时作为最终降级方案
- 不向用户展示原始 JSON，数据通过可视化页面呈现
- 驱动内置重试机制（3次指数退避），常见错误附带 `_hint` 中文提示

## 对话收尾提示（必须执行）

**每完成一轮完整对话（任务完成或用户没有后续请求）后，必须主动提示用户接下来可以做什么。** 禁止以单纯确认结束对话（如只说"好的/已处理"）。

提示方式（结合刚完成的动作，选最相关的 2~3 个）：
- 刚搜完品 → 「已找到 N 款，需要我帮你挑几款发布到店铺吗？或换个关键词再搜？」
- 刚发布完 → 「发布已提交，需要查看发布结果？或继续发下一批？」
- 刚改完价 → 「价格已更新，需要看下仪表盘确认效果？或批量优化标题？」
- 刚处理完订单/退款 → 「已处理完，还有其他待发货/待退款要一起处理吗？」
- 刚做完标题/SEO → 「需要批量应用到其他商品吗？或做全店标题巡检？」
- 用户刚安装/首次对话 → 展示能力总览：「我可以帮你搜新品、极速发布、管理店铺、处理订单/评价/退款，或 AI 优化标题，想从哪个开始？」

## MCP 就绪检查

安装后先检查本地 MCP 配置（`~/.workbuddy/mcp.json`、`~/.claude.json`、`~/.config/opencode/opencode.jsonc` 等）中是否有实例名以 `开山网*` / `包牛牛*` 前缀命名的 MCP 连接：

- **有** → 直接使用，进入功能路由
- **没有** → 提示用户：「当前未配置开山网/包牛牛 MCP，内测阶段请联系客服索取你的 API token」，并给出配置引导（服务地址 `https://mcp.k3.cn/mcp`，请求头 `X-API-Key`）

## 环境与 Config

检查 Python ≥ 3.8，检查 `~/.config/k3-publish/config` 中目标平台的 Key。未配置则引导输入。

不同平台的 Key 互不通用，格式：`API_KEY_k3=...` / `API_KEY_bao66=...`

## 功能路由表

根据用户触发的功能关键词，按需加载对应模块文件：

| 功能类别 | 触发词示例 | 模块文件 |
|----------|-----------|----------|
| 货源搜品/今日新款 | 搜品、选品、今日新款、搜索、今天有什么 | `modules/selection.md` |
| 极速发布/发布记录 | 发布、极速发布、上架、publish | `modules/publish.md` |
| 淘宝店铺运营 | 标题优化、SEO诊断、利润分析、经营问答、属性补全、选品日历、详情文案、改价、发货、退款、评价管理 | `modules/taobao-ops.md` |

用户未指定操作时，弹出菜单：今日新款 / 搜索产品 / 店铺列表 / 极速发布 / 发布记录 / 淘宝店铺管理 / 快捷命令。

## 数据展示约束

| 约束 | 说明 |
|------|------|
| 平台标识 | 吸顶栏红色粗体平台名，滚动不消失 |
| 移动优先 | 默认 2 列，768px+ → 4 列，1024px+ → 5 列 |
| 商家&货号 | 每款一行 `商家名&货号` |
| 价格前置 | 毛玻璃黑底浮于图片左下角 |
| 图片嵌入 | base64 内嵌，不引远程 URL |

## 搜索安全规则

关键词限制 2~20 字符：不足 2 字符拒绝+提示，超过 20 字符自动截断。输入自动去除 HTML 标签和控制字符。

## 常见问题

- **搜索关键词不够？** 需 ≥ 2 字符，超 20 自动截断。
- **店铺不存在或没有权限？** `shops` 查看已授权店铺列表。
- **网络波动报错？** 驱动自动重试 3 次，无需手动处理。
- **看不懂错误？** 响应附带 `_hint` 字段给出中文提示。
- **快捷命令？** `dashboard` 仪表盘 / `daily-report` 日报 / `profit-analysis` 利润分析 / `auto-ship` 批量发货 / `batch-price` 批量调价 / `batch-title` 标题优化 / `title-check` 标题巡检 / `rate-check` 差评告警。完整列表见 `python scripts/driver.py --help`。
- **功能归属？** 搜品/选品 → `modules/selection.md`，发布 → `modules/publish.md`，淘宝店铺运营 → `modules/taobao-ops.md`。

## API 调用策略

| 优先级 | 方式 | 适用模块 | 说明 |
|--------|------|----------|------|
| 1 | **MCP Tool** (聚源百成MCP) | selection, publish | 已就绪 5 个 Tool（search_products / get_today_new / get_shops / fast_publish / check_publish_result） |
| 2 | **driver.py** | 所有模块 | MCP 不可用时的降级方案，taobao-ops 模块当前仍以此为主 |
| 3 | **curl** | 所有模块 | Python 不可用时的最终降级 |

taobao-ops 模块已完成 25 个 MCP Tool 开发，可按 Tool 就绪情况逐个接入。

## 参考文档时效性

命令列表以 `python scripts/driver.py --help` 为唯一权威来源，始终与服务端同步。`references/` 为补充说明，可能滞后于实际实现。遇到不一致时以 `--help` 输出为准。

## 模块加载规则

- 根据用户意图只加载对应的 `modules/*.md` 文件，不加载无关模块
- 跨模块操作（如"搜品→发品→优化标题"）由 AI 按需依次加载多个模块，用户无感知切换
- 新增平台只需：新建 `modules/<platform>-ops.md` + `agents/<platform>/` + `references/<platform>/`
