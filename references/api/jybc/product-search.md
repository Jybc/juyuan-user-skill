# GET /product/search

搜索产品。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| wd | string | 是 | 搜索关键词（需 URL 编码，2~20 字符） |
| platform | string | 否 | k3 / bao66，默认 k3 |

每次最多返回 20 个产品。输入自动做安全处理（去除 HTML 标签和控制字符）。
